#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: parameter for an integer
 * @y: parameter for an integer
 *
 * Return: sum of the two integers
 */

int add(int x, int y)
{
	return (x + y);
}
